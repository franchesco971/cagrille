#payum:
#    gateways:
#        offline:
#            factory: offline

payum:
    gateways:
        paypal_express_checkout:
            factory: sylius_paypal
            client_id: '%env(PAYPAL_CLIENT_ID)%'
            client_secret: '%env(PAYPAL_CLIENT_SECRET)%'
            sandbox: true # ou false pour le mode Live
